<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <!-- Icono simple de "pulso/latido" para la notificacion persistente. Los iconos de
         notificacion en Android DEBEN ser monocromaticos (solo alpha), por eso es un trazo
         simple sin relleno de color, el sistema lo pinta con el color del tema. -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M3,12h4l2,-7l4,14l2,-9l2,4h4"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
